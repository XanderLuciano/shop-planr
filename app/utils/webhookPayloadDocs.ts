/**
 * Webhook payload documentation derived from Zod schemas.
 * Auto-imported by Nuxt — available in all components.
 *
 * Re-exports the schema registry and field extraction utility from
 * the server schemas layer. These are pure Zod definitions with no
 * server runtime dependencies, safe for client-side use.
 */
export { WEBHOOK_PAYLOAD_SCHEMAS, extractPayloadFields } from '../../server/schemas/webhookPayloadSchemas'
